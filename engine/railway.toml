[build]
builder = "nixpacks"

[deploy]
startCommand = "python -m uvicorn wisdom_engine:app --host 0.0.0.0 --port ${PORT:-3002}"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
