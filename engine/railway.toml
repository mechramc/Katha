[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "python -m uvicorn wisdom_engine:app --host 0.0.0.0 --port 8080"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
