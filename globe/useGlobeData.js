/**
 * useGlobeData.js — Vault API feed hook for the globe.
 *
 * Fetches passport data from the Vault and transforms it
 * into the format needed by the Globe component.
 *
 * All Vault API calls go through useVault() — no direct fetch().
 */
